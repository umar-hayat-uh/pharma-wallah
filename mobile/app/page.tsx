import ToolHub from "./_components/ToolHub";

// The app opens straight on the catalogue — there is no marketing home screen
// inside the APK.
export default function HomePage() {
  return <ToolHub />;
}
