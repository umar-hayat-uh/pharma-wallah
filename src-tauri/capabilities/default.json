{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The minimum an offline calculator needs: its own window, and nothing else. No shell, no network, no filesystem plugin, no updater, no clipboard plugin, no dialog plugin. The four commands this app defines (store_load, store_save, store_path, export_save) are application commands, which are reached through invoke_handler rather than through a capability, and each one constrains what it will touch in src/lib.rs.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
